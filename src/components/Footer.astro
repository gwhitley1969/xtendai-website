---
const currentYear = new Date().getFullYear();

const navLinks = [
  { label: 'Products', href: '/products' },
  { label: 'About', href: '/about' },
  { label: 'Support', href: '/support' },
  { label: 'Contact', href: '/contact' }
];

const legalLinks = [
  { label: 'Privacy', href: '/privacy' },
  { label: 'Terms', href: '/terms' }
];
---

<footer class="footer">
  <div class="footer__glow"></div>
  <div class="footer__container container">
    <div class="footer__top">
      <div class="footer__brand">
        <a href="/" class="footer__logo" aria-label="Xtend-AI Home">
          <img src="/images/Xtend-AI_logo_primary_white.png" alt="Xtend-AI" width="140" height="38">
        </a>
        <p class="footer__tagline">Your next favorite app, made smarter</p>
      </div>

      <nav class="footer__nav" aria-label="Footer navigation">
        <ul class="footer__list">
          {navLinks.map((link) => (
            <li>
              <a href={link.href} class="footer__link">{link.label}</a>
            </li>
          ))}
        </ul>
      </nav>
    </div>

    <div class="footer__bottom">
      <p class="footer__copy">
        &copy; {currentYear} Xtend-AI. All rights reserved.
      </p>
      <ul class="footer__legal">
        {legalLinks.map((link) => (
          <li>
            <a href={link.href} class="footer__legal-link">{link.label}</a>
          </li>
        ))}
      </ul>
    </div>
  </div>
</footer>

<style>
  .footer {
    position: relative;
    background: var(--xt-bg-secondary);
    padding: var(--space-16) 0 var(--space-8);
    overflow: hidden;
  }

  .footer__glow {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 200px;
    background: radial-gradient(ellipse at center, rgba(139, 92, 246, 0.1) 0%, transparent 70%);
    pointer-events: none;
  }

  .footer__container {
    position: relative;
    z-index: 1;
  }

  .footer__top {
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
    padding-bottom: var(--space-8);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  @media (min-width: 768px) {
    .footer__top {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
    }
  }

  .footer__brand {
    max-width: 300px;
  }

  .footer__logo img {
    height: 32px;
    width: auto;
  }

  .footer__tagline {
    margin-top: var(--space-3);
    font-size: 0.9375rem;
    color: var(--xt-text-muted);
    margin-bottom: 0;
  }

  .footer__nav {
    margin-top: var(--space-4);
  }

  @media (min-width: 768px) {
    .footer__nav {
      margin-top: 0;
    }
  }

  .footer__list {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4) var(--space-8);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer__link {
    color: var(--xt-text-secondary);
    font-weight: 500;
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .footer__link:hover {
    color: var(--xt-purple-light);
  }

  .footer__bottom {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    padding-top: var(--space-8);
  }

  @media (min-width: 768px) {
    .footer__bottom {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }
  }

  .footer__copy {
    font-size: 0.875rem;
    color: var(--xt-text-muted);
    margin: 0;
  }

  .footer__legal {
    display: flex;
    gap: var(--space-6);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer__legal-link {
    font-size: 0.875rem;
    color: var(--xt-text-muted);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .footer__legal-link:hover {
    color: var(--xt-purple-light);
  }
</style>
